# Socket.IO Server URL
# For local development, use http://localhost:5001
# For production, use your deployed Socket.IO server URL (e.g., Railway, Render, Heroku)
NEXT_PUBLIC_SOCKET_URL=http://localhost:5001
